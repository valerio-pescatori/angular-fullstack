import { DropdownCastPipe } from './dropdown-cast.pipe';

describe('DropdownCastPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownCastPipe();
    expect(pipe).toBeTruthy();
  });
});
